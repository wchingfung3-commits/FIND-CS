# Public project URL and publishable/legacy anon key ONLY. Never use service_role here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Keep demo until migration and RLS tests have passed. Set to live to use published DB records.
VITE_CATALOG_MODE=demo
